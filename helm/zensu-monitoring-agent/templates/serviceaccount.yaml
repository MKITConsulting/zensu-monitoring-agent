{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zensu-monitoring-agent.serviceAccountName" . }}
  labels:
    {{- include "zensu-monitoring-agent.labels" . | nindent 4 }}
{{- end }}
